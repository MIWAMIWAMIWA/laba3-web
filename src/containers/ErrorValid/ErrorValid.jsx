import React from 'react';

const ErrorValid = ({ children }) => (
    <div className="error-main" style={{ color: 'red', fontSize: '14px', marginTop: '5px' }}>{children}</div>
);

export default ErrorValid;